def input_error(func): 
    def inner(*args, **kwargs): # Декоратор
        try:
            return func(*args, **kwargs)
        except ValueError:
            return "Give me name and phone please."
        except KeyError:
            return "Contact not found."
        except IndexError:
            return "Enter the argument for the command."
    return inner

def parse_input(user_input):
    if not user_input.strip():
        return "", []
    cmd, *args = user_input.split()
    cmd = cmd.strip().lower()
    return cmd, *args

@input_error
def add_contact(args, contacts): # Якщо аргументів менше 2, виникне ValueError 
    name, phone = args
    contacts[name] = phone
    return "Contact added."

@input_error
def change_contact(args, contacts):# Якщо аргументів менше 2, виникне ValueError  
    name, phone = args
    if name in contacts:
        contacts[name] = phone
        return "Contact updated."
    else:  # Генеруємо KeyError якщо контакт не знайдено
        raise KeyError

@input_error
def show_phone(args, contacts):  # Якщо args порожній, виникне IndexError 
    name = args[0]
    if name in contacts:
        return contacts[name]
    else:
        raise KeyError

@input_error
def show_all(contacts):
    if not contacts:
        return "No contacts found."
    result = []
    for name, phone in contacts.items():
        result.append(f"{name}: {phone}")
    return "\n".join(result)

def main():
    contacts = {}
    print("Welcome to the assistant bot!")
    while True:
        user_input = input("Enter a command: ")
        command, *args = parse_input(user_input)
        # Обробляємо всі команди
        if command in ["close", "exit"]:
            print("Good bye, my dear!")
            break
        
        elif command == "hello":
            print("How can I help you?")
        
        elif command == "add":
            print(add_contact(args, contacts))
        
        elif command == "change":
            print(change_contact(args, contacts))
        
        elif command == "phone":
            print(show_phone(args, contacts))
        
        elif command == "all":
            print(show_all(contacts))
        
        elif not command:
            continue
        
        else:
            print("Invalid command.")

if __name__ == "__main__":
    main()